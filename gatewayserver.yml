server:
  port: 8072

eureka:
  instance:
    prefer-ip-address: true  # Fix: Changed to correct format

  client:
    register-with-eureka: true  # Fix: Changed to correct format
    fetch-registry: true
    service-url:
      defaultZone: "http://localhost:8070/eureka/"
